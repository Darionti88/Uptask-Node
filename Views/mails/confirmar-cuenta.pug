<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h2 Confirma tu cuenta Uptask
        p Ya casi! Confirmá tu cuenta acá
        a(href=`${confirmarUrl}`) Confirma tu cuenta
        p Si no puedes acceder visita #{confirmarUrl}
        p Si no solicitaste este e-mail puedes ignorarlo.